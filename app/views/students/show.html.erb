<div class="container">
  <div class="card card-box">
    <div class="row">
      <div class="col">
        <h1><%= @student.last_name %> <%= @student.first_name %></h1>
      </div>
      <div class="col text-right">
        <%= link_to 'Editar', edit_course_student_path(@student.course, @student), class: "btn btn-secondary" %>
        <%= link_to 'Atrás', course_students_path(@student.course), class: "btn btn-secondary" %>
      </div>
    </div>
    <hr>
    <p>
      <b>DNI:</b>
      <%= @student.dni %>
    </p>

    <p>
      <b>Legajo:</b>
      <%= @student.number %>
    </p>

    <p>
      <b>Email:</b>
      <%= @student.email %>
    </p>
  </div>
</div>



