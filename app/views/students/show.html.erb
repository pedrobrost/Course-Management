<p id="notice"><%= notice %></p>

<div class="container">
  <div class="card card-box">
    <div class="row">
      <div class="col">
        <h1><%= @student.last_name %> <%= @student.first_name %></h1>
      </div>
      <div class="col text-right">
        <%= link_to 'Edit', edit_course_student_path(@student.course, @student), class: "btn btn-secondary" %>
        <%= link_to 'Back', course_students_path(@student.course), class: "btn btn-secondary" %>
      </div>
    </div>
    <hr>
    <p>
      <b>Dni:</b>
      <%= @student.dni %>
    </p>

    <p>
      <b>Number:</b>
      <%= @student.number %>
    </p>

    <p>
      <b>Email:</b>
      <%= @student.email %>
    </p>
  </div>
</div>



